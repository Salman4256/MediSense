package com.medisense.app.data.sync.model

/**
 * Health entity categories supported by the synchronization engine.
 */
enum class SyncEntityType {
    HEALTH_PROFILE,
    MEDICATIONS,
    MEDICATION_HISTORY,
    APPOINTMENTS,
    PREDICTION_HISTORY
}
